# 努尔意见改版执行计划与验收标准

> 目标：先把改版方向、开发顺序、验收口径固定下来，再进入代码修改。
> 来源：2026-05-25 能源部 demo & 方案研讨会议记录、`CONTEXT.md`、现有页面实现审查。
> 演示对象：哈萨克斯坦能源部副部长 + 数字化司。
> 演示讲法：详见 `能源部Demo演示故事线.md`，所有页面改造必须服务该故事线。

---

## 1. 改版总原则

本轮不是做一个更炫的能源 BI 大屏，而是把 demo 改成一个副部长能理解、能推动、能向上汇报的监管智能体原型。

必须始终把“事前预警 / 防患于未然”放在主价值位。能源行业的核心不是事故后复盘，而是在事故、供给波动、违规扩大和问责发生前，把风险识别出来并推动处置。

所有页面改动都必须通过 5 个问题：

1. 这一屏是不是副部长/数字化司想看的，而不是企业经理或工程师想看的？
2. 它表达的是事前预警，还是已经发生后的复盘？如果是复盘，是否明确降级为历史模式库/证据库？
3. 关键数字有没有置信区间、回测准确率、方法说明或 demo 模拟说明？
4. 这一屏有没有唯一主重点？用户 3 秒内能不能知道该看哪里？
5. 从发现到归因、分派、处置、审计、报告，故事是否闭环？

### 1.1 拆页原则

如果一屏信息量过大，优先拆页或下钻，不通过更小字号、更密卡片、更复杂布局来硬塞。

拆页的判断标准：

- 主页面只保留“结论 + 动作 + 关键数字”，服务副部长 3 秒决策。
- 证据、模型、算法、原始明细、长列表进入 drill-down 页面、抽屉或折叠区。
- 拆页后必须能沿原故事线返回，不能变成平行功能展示。
- 页面标题要直接回答监管问题，例如“未来什么时候会出事”“为什么判断它有风险”“谁来处理”。
- 每个主页面保留一个主视觉；如果出现两个同等重要的大模块，就拆。

建议拆法：

| 当前页面 | 主页面保留 | 拆出/下钻 |
|---|---|---|
| `NationalGrid` | 国家风险态势、重点风险区域、总部待处理事项 | 完整事件队列、能源结构统计、区域设施明细 |
| `PipelineTimeSeries` | 预测风险、处置窗口、损失暴露、AI 监管影响 | 模型对比、完整时序诊断、相似案例库 |
| `EnterpriseReporting` | 跨系统证据链结论、模式匹配、建议核查 | 各系统原始凭证、财务/排放/报送明细 |
| `WorkflowAttribution` | 主因判断、预防干预计划、责任部门 | 6-agent 全量证据、知识图谱、历史案例路径 |
| `EventAudit` | SLA 时间线、超时环节、问责状态 | 全量生命周期矩阵、节点证据抽屉 |
| `ReportGeneration` | 部长简报、待决策事项、引用来源 | 报告编辑器、导出设置、完整引用清单 |

已落地的第一步拆分：

- `PipelineDecision.tsx`：承载 `/warning/timeseries/:anomalyId` 主线页，只讲风险、处置窗口、监管动作，突出事前预警价值。
- `PipelineTimeSeries.tsx`：改为 `/warning/timeseries/:anomalyId/diagnostics` 诊断下钻页，保留模型对比、时序图和相似案例。
- `EventAuditSla.tsx`：承载 `/audit/event/:caseId` 主线页，只讲预防处置 SLA、防患窗口和瓶颈。
- `EventAudit.tsx`：改为 `/audit/event/:caseId/matrix` 审计矩阵下钻页。

---

## 2. Demo 主线

保留现有 4 幕结构，但重新压缩为一条监管闭环故事：

```text
Act I  全景风险感知
副部长打开系统，先知道国家能源供给是否稳定、哪里有风险、哪个事项需要总部处置。

Act II  事前预警与归因
AI 不只是发现异常，而是预测未来 48H/90D 风险，并解释为什么、证据是什么、与历史违规模式是否相似。

Act III  监管处置闭环
系统把风险分派给责任部门，形成现场核查、整改、复核、审计留痕，重点是在风险窗口内完成预防性处置，避免事故或违规扩大。

Act IV  闭环报告输出
向部长回答：AI 发现了什么、为什么这样判断、谁在处理、处置是否留痕、如何向上汇报。
```

---

## 3. 执行计划

### P0-1 首页改为 Minister Command View

**文件**
- `src/pages/NationalGrid.tsx`
- 可能涉及 `src/components/UI.tsx`

**当前问题**
- 左右固定面板抢主视觉，地图不是唯一焦点。
- KPI 已经有副部长视角雏形，但仍偏数据展示。
- 图层切换和图例使用 emoji/小字号，视觉不够正式。

**改法**
- 左侧统计面板默认折叠，仅保留 44px 左侧 rail；点击展开详细能源结构。
- 右侧风险事件默认半折叠，只显示 Critical 摘要；点击展开事件列表。
- KPI 从纯指标改为 4 个监管问题：
  - Supply Stability
  - High-Risk Events
  - Pending Decisions
  - Avoided Loss / Exposure
- 地图只突出高风险区域和关键能源节点，正常基础设施降噪。
- 15 分钟表述改为分层：Telemetry `<1s capable` / SCADA reporting `15-min current feed` / AI inference `continuous`.
- 移除 tab 图标里的 emoji，换成 Lucide 或纯文本标签。

**验收标准**
- 首屏 3 秒内能回答：国家能源供给是否稳定、哪里有风险、需要总部处理什么。
- 地图区域在 1440px 宽度下占主视觉，不被左右面板压缩到次要位置。
- 左右面板可折叠，折叠按钮有可见 label/aria-label，点击区域不小于 44px。
- 首页不再把 `15-Min` 表达成系统实时能力限制。
- 不出现 emoji 作为结构性图标。

---

### P0-2 PipelineTimeSeries 改成事前预警页

**文件**
- `src/pages/PipelineTimeSeries.tsx`
- `src/data/anomaly/ano_0512_metadata.ts`
- `src/data/anomaly/anomaly_queue.ts`

**当前问题**
- 已经有预测文案，但页面中心仍是算法选择、时序图、队列，AI 结论在右栏。
- “15 MIN”容易再次触发努尔关于毫秒级/报送周期的挑战。
- AI 直接带来的监管能力没有直观呈现。

**改法**
- 页面顶部新增大号决策卡：
  - `Predicted breach risk: 92% within 48H`
  - `Action deadline: 36H`
  - `Potential avoided loss: 75 MMcm / 30D`
  - `Confidence: 95% CI + backtest accuracy`
- 时序图保留，但降为证据区；明确 NOW 左侧是历史观测，右侧是预测区间。
- 算法选择改为折叠或小面板，不作为主视觉。
- 增加 `AI Regulatory Impact` 模块：
  - continuous risk scoring
  - cross-system pattern match
  - action window generated
  - responsible owner suggested
- 15 分钟解释为当前 SCADA 上报频率，不是 AI 系统能力边界。

**验收标准**
- 页面第一眼看到的是“未来风险 + 最晚处置窗口”，不是算法控件。
- 所有风险百分比、偏差、相似度必须带 CI、回测或方法说明。
- 有明确的损失规避指标，能回答“能源部为什么关心”。
- AI 对监管动作的直接影响能在一屏内读懂。

---

### P0-3 WorkflowAttribution 增加预防干预节点

**文件**
- `src/pages/WorkflowAttribution.tsx`
- `src/data/attribution/case_001_attribution.ts`

**当前问题**
- 6 条 lane 和 Master Agent 偏事后归因。
- “Recommended actions” 还不够像部长关心的预防处置计划。

**改法**
- 在 Master Agent Verdict 后新增 `Preventive Intervention Plan`。
- 数据层新增字段：
  - 90D safety incident probability
  - recommended intervention window
  - responsible owner
  - dispatch SLA
  - expected avoided loss / avoided incident type
- UI 中把 “INITIATE CASE” 改成更明确的监管动作，例如 `Dispatch Preventive Inspection`。
- 保留历史证据链，但文案说明历史案例用于模式匹配，不是本页主目的。

**验收标准**
- 页面能清楚表达：AI 归因之后，系统下一步建议监管者做什么。
- 不只显示 “unreported capacity expansion”，还显示未来风险、处置窗口、责任部门。
- Master Agent 输出与闭环阶段能连接到 EventAudit/ReportGeneration。

---

### P1-1 EventAudit 拆为防患 SLA 主线页 + 矩阵下钻页

**文件**
- `src/pages/EventAudit.tsx`
- `src/pages/EventAuditSla.tsx`
- `src/data/audit/case_001_lifecycle_matrix.ts`

**当前问题**
- 矩阵完整，但部长不一定能看出瓶颈在哪里。
- 努尔提到要全局看到流程，而不是单点 OA 串联。

**改法**
- 默认进入 `EventAuditSla`，主视觉为 `Preventive SLA Timeline`：
  - 每阶段 expected vs actual
  - 36H 防患窗口剩余时间
  - overdue hours
  - owner
  - AI bottleneck flag
- `EventAudit` 原矩阵保留为 `/audit/event/:caseId/matrix` 审计证据下钻页。
- 增加一条闭环摘要：Detected -> Attributed -> Dispatched -> Inspected -> Rectified -> Reviewed -> Archived。

**验收标准**
- 用户能一眼看出防患窗口是否还来得及、哪个阶段超时、谁负责、超时多久。
- 矩阵不是“看起来复杂的流程图”，而是能支撑问责和审计。
- 点击节点仍可查看证据和来源。

---

### P1-2 ReportGeneration 强化部长简报闭环

**文件**
- `src/pages/ReportGeneration.tsx`

**当前问题**
- 现在像报告生成器，没把前面 AI 发现、证据链、处置状态和审计留痕沉淀成部长可汇报材料。

**改法**
- 增加或改造模块：`Minister Briefing Output`。
- 内容包括：
  - AI risk finding
  - evidence chain summary
  - preventive intervention status
  - SLA / audit trail summary
  - pending minister decision
  - source references

**验收标准**
- 能回答：副部长如何把这个风险向上汇报。
- 能回答：AI 判断、证据、处置、留痕分别来自哪里。
- 报告不新增无法追溯的结论。

---

### P1-3 全局 UI/UX 清理

**文件**
- `src/index.css`
- `src/components/UI.tsx`
- `src/components/LeftMenu.tsx`
- 所有 P0 页面

**当前问题**
- 大量 8px/9px 字体，部长级演示不可读。
- emoji 作为结构性 icon。
- 点击目标偏小，部分按钮缺 aria-label。
- 信息密度过高。

**改法**
- 正文/关键说明最低 11-12px，部长主结论 18-28px。
- 结构图标统一用 Lucide 或纯文字，不用 emoji。
- 可点击控件最小高度 36px，重要折叠/操作按钮 44px。
- 每个页面保留一个主视觉，辅助面板可折叠。
- 保持浅色政务监管风格，不引入大面积炫酷暗黑视觉。

**验收标准**
- 1440px 桌面演示时，主要结论不需要凑近看。
- tab、折叠、返回、导出、启动流程等按钮有明显 hover/focus/disabled 状态。
- 不出现横向溢出导致主内容被遮挡。

---

## 4. 开发顺序

建议按下面顺序实现，避免在细节页面里先陷进去：

1. `NationalGrid.tsx`：先把第一屏改成能过努尔第一关。
2. `PipelineTimeSeries.tsx`：把事前预警和 AI 价值差讲清楚。
3. `WorkflowAttribution.tsx` + attribution data：把预防干预节点补上。
4. `EventAudit.tsx`：补时限追踪，形成问责闭环。
5. `ReportGeneration.tsx`：补部长简报与闭环输出。
6. 全局 UI/UX 清理：字号、图标、交互态、折叠面板一致性。

---

## 5. 最终验收清单

### 叙事验收

- [ ] 讲 demo 时可以只按一条故事线走，不需要解释 8 个功能点。
- [ ] 每一幕都能对应核心关键词：监管者视角、AI 事前预警、证据链、处置闭环、审计留痕。
- [ ] 所有主线页都能回答“这个 AI 能不能帮助能源部在事故或违规扩大前防患于未然”。
- [ ] 历史案例只作为模式库/证据链，不再成为主叙事。

### 页面验收

- [ ] 首页首屏能回答“今天国家能源系统有没有事”。
- [ ] 预警页首屏能回答“未来什么时候会出事、我还有多久能干预”。
- [ ] 归因页能回答“为什么判断这个风险、下一步谁处理”。
- [ ] 审计页能回答“哪个环节卡住、是否合规留痕”。
- [ ] 报告页能回答“副部长如何向上汇报、结论是否可追溯”。

### 数据可信验收

- [ ] 所有概率、偏差、相似度、损失规避数字均有 CI、回测、方法说明或 demo 模拟说明。
- [ ] 不把 15 分钟说成能源系统实时能力，只说成当前上报频率。
- [ ] Pavlodar/Ekibastuz 最大煤矿区仍保留并明显可见。

### UI/UX 验收

- [ ] 每屏只有一个主视觉重点。
- [ ] 左右辅助面板不压制主内容，必要时默认折叠。
- [ ] 无 emoji 结构图标。
- [ ] 核心文字在演示屏上可读。
- [ ] 交互控件有 hover/focus/pressed 状态，重要按钮点击区不小于 44px。

### 技术验收

- [ ] `npm run lint` 通过。
- [ ] `npm run build` 通过。
- [ ] `npm run dev` 后手动走通主线：NationalGrid -> PipelineTimeSeries -> WorkflowAttribution -> EventAudit -> ReportGeneration。
- [ ] 浏览器检查 1440px 和 1024px 宽度下无明显遮挡、横向溢出或关键内容被固定栏盖住。
