# 努尔批评意见与修改方案

> 来源：2026-05-25 能源部 Demo & 方案研讨会（彦泽 demo + 世昆 demo 联合评审）
> 目标受众：哈萨克斯坦能源部副部长 + 数字化司
> 改完截止：周四/周五（2026-05-28/29）演示

---

## 一、根本性问题诊断

努尔的批评可归为三个根因，其他所有具体意见都是这三个根因的衍生：

| # | 根因 | 严重程度 |
|---|------|----------|
| A | **视角错位** — 做的是企业级 BI，不是部委级监管 | 致命 |
| B | **时序错位** — 全是事后追溯，没有事前预警 | 致命 |
| C | **故事线散** — 8 个页面是功能点陈列，不是一条闭环故事 | 严重 |

---

## 二、逐条批评 → 对齐 → 修改方案

### 2.1 数据层面

#### 批评 1：Pavlodar 最大煤矿区缺失

> "PA 开头的，这一块有一个很大的最大的哈萨克最大的煤矿区就在这，但是你们没覆盖到。最大的。数据中心会建在这里。"

**涉及文件**：`src/data/coal.ts`

**修改方案**：
- 在 `COAL_BASINS`、`COAL_MINES` 中补充 Pavlodar/Ekibastuz 区域数据点
- 增加 Ekibastuz GRES-1/GRES-2 煤电基地标注
- 地图上该区域用明显视觉权重区分（最大矿区 = 最大图标/最深色）

#### 批评 2：15 分钟粒度过粗

> "15分钟对于能源行业是不可接受的，它是毫秒级的"

**涉及页面**：`NationalGrid.tsx`（Context Bar 显示 "15-Min Cycle"）

**修改方案**：
- 将 "15-Min Cycle" 改为分层表述：
  - 遥测采集层：`<1s 实时`
  - AI 推理层：`15min 批量`（这是现有 SCADA 上报周期，我们在此之上做推理）
- 在 PipelineTimeSeries 中标注 15min 是报送频率，不是系统限制——"基于现有 SCADA 15min 上报周期做推理，接入毫秒级数据后可实时预警"

---

### 2.2 视角层面（最核心的修改）

#### 批评 3：没有站在监管者角度

> "场景是优先得从监管者的角度……一定是细化他的监管的职责边界权、对象以及流程"
> "不是 to B 的业务，to B 是单点，国家层面的部委级别的是一个面，是一个完整的闭环"

**涉及全局架构**。

**修改方案——重构首页 NationalGrid**：

当前首页是一个纯数据展示的地图 + 统计面板。应按**副部长视角**重新设计信息架构：

```
第一屏（副部长 Dashboard）应回答三个问题：
1. 国家能源供给是否稳定？（电/气/油供给健康度）
2. 哪里有风险？（高风险区域/企业/事件）
3. 需要我做什么？（待处置事项/决策点）
```

具体改动：
- 新增顶部 KPI 条：供给稳定指数 / 本周异常事件数 / 待处置高风险事件 / 合规率
- 地图上高亮风险区域（红色脉冲），正常区域弱化
- 右侧事件 feed 从纯日志改为**分级风险事件**：严重/警告/关注，每条可点击下钻
- 左下角增加 "今日待处置" 快捷入口

#### 批评 4：没有对比 "有 AI vs 没 AI" 的价值差

> "我拥有了你的东西，他能帮我做什么事？他没有跟现有的比对出来，我们的客户价值也没有呈现出来"

**修改方案**：
- 在异常预警页面（PipelineTimeSeries / EnterpriseReporting）增加对比模块：
  - 传统方式：人工比对 5 个系统数据 → 平均 72 小时发现异常
  - AI 方式：跨系统数据自动关联 → 实时发现 + 推理归因
- 用具体数字说话："过去 12 个月发现 27 起潜在违规，此前人工审核仅发现 3 起"

#### 批评 5：效率优势不是客户关心的

> "你就算有这个效率，节省了多少成本，能源部他并不关心……它并没有转化成我对我这个监管者而言，我的监管职责内的 KPI"

**修改方案**：
- 所有话术/界面文字从 "我们多快" 转为 "帮监管者达成了什么"
- 示例改写：
  - 旧：1 周快速交付
  - 新：从数据上报到监管决策从 72 小时缩短到 24 小时
  - 旧：AI 自动识别
  - 新：覆盖 100% 企业数据，零遗漏

---

### 2.3 时序层面（事前 vs 事后）

#### 批评 6：系统是事后的，不是事前预警

> "安全事件已经发生了……我的系统为什么不能提前去判断这一类的事件有可能会发生？"
> "所有的东西都得前置的情况下"

**这是努尔重复最多次的批评**，涉及 PipelineTimeSeries、EnterpriseReporting、WorkflowAttribution 三个核心页面。

**涉及文件**：
- `src/pages/PipelineTimeSeries.tsx`
- `src/pages/EnterpriseReporting.tsx`
- `src/pages/WorkflowAttribution.tsx`
- `src/data/anomaly/ano_0512_metadata.ts`

**修改方案**：

**PipelineTimeSeries 页面**：
- 叙事逻辑从 "发现历史异常" 改为 "预测未来风险"
- 时序图默认展示**未来预测区间**（Future Risk Forecast 从侧边栏提到主视觉中心）
- 标注 "NOW" 线，左侧是历史观测，右侧是 LLM 预测 + 置信带
- 告警文案从 "检测到偏差 +32%" 改为 "预测未来 48H 内 92% 概率发生超标事件"
- Hypothesis 卡片增加 "建议处置窗口"：最晚何时必须行动

**EnterpriseReporting 页面**：
- 叙事逻辑从 "发现企业数据不一致" 改为 "识别出潜在违规行为模式"
- 增加 "模式匹配" 模块：当前数据模式 vs 已知违规模式库的相似度
- 文案：从 "发现 SCADA 偏离 +15.7%" 改为 "运行模式与已确认的超产违规案例匹配度 0.87，建议启动核查"

**WorkflowAttribution 页面**：
- 增加 "前置干预" 节点：在 Agent 发现归因后，增加 "建议预防措施" 输出
- 不只是说 "未申报产能扩张"，而是说 "类似模式在未来 90 天内有 68% 概率导致安全事故"

#### 批评 7：能源事故是不可逆的

> "能源是有一次能源跟二次能源，一次能源浪费了就浪费了……不可逆的一个损失"

**修改方案**：
- 在首页或预警页增加 "潜在损失规避" 指标
- 示例：如未预警，该异常持续 30 天将导致 XXX 方天然气浪费 / XXX 吨碳排放超标
- 强调 AI 预警的**止损价值**

#### 批评 8：数字缺乏科学严谨性

> "能源系统就工程类的里边，它是非常严谨的，指标的背后，必须得是 100% 科学的"
> "32% 的置信概率只有 60% 的情况下，不如不用给这个参考的数值"

**涉及文件**：`src/data/anomaly/ano_0512_metadata.ts`（kpis 字段）

**修改方案**：
- 去掉没有置信区间的单一数字
- 每个预测值附带：方法说明 + 置信区间 + 历史回测准确率
- 示例：不要只显示 "+32% 偏差"，改为 "预测偏差 32%（95% CI: 28-36%，历史 30 天回测准确率 91%）"
- 如果数值是 demo 数据，标注为 "基于历史案例模拟"

---

### 2.4 视觉与体验

#### 批评 9：界面不够极简

> "整个视觉并不美观，不够好，不够极简。你作为一个部长的话，你会看一个非常复杂的一个界面吗？"

**涉及页面**：全局 CSS + 所有页面

**修改方案**：
- 减少每屏信息密度——每屏只回答一个问题
- Header/Context Bar 精简：去掉版本号、去掉冗余标签，只保留核心面包屑
- 地图页面：默认隐藏左侧统计面板 + 右侧事件 feed → 折叠按钮唤起
- 颜色系统精简：警告/严重/正常三色足矣，减少装饰色
- Font size 统一上调一级（当前大量 8px/9px 文字，部长级别用户不可读）

#### 批评 10：抓不住重点，太散

> "在你的整个界面当中，我抓不住重点"

**修改方案**：
- 每个页面必须有**唯一的主视觉焦点**（通常是地图/图表/矩阵的中心区域）
- 侧边栏面板全部可折叠，默认只展示结论卡片
- PipelineTimeSeries：把 AI 推理结论从右侧 360px 侧栏提到正中央，图表缩小为支撑证据

---

### 2.5 故事线

#### 批评 11：场景选的不是部长关注点

> "你们的场景选的是公司的业务，而不是能源部他的，与他的副部长的视觉跟部长的视觉，如何把监管做成一个系统的角度出发的"

**全局重构方案——重新设计故事线**：

**旧故事线（当前 demo）**：
```
感知屏 → 区域设施 → 管线时序 → 企业报表 → 归因工作流 → 知识图谱 → 审计矩阵 → 报告生成
（散点陈列，8 个独立功能）
```

**新故事线（副部长视角的闭环）**：
```
第一幕：全景风险感知（1 屏）
  "我是副部长，打开系统，3 秒知道今天有没有事"
  → 供给稳定性 / 高风险事件数 / 待我处置的事项
  → 一张地图，红点 = 需要我关注的

第二幕：从告警到归因（2 屏）
  "系统告诉我哪里有问题，为什么有问题"
  → 异常自动识别 + AI 推理归因 + 历史案例匹配
  → 不只是"发现"，而是"预测 + 解释 + 建议"

第三幕：监管闭环（2 屏）
  "问题分派给谁，处理了没有，留痕了没有"
  → 处置工单流转 + 审计矩阵 + 时限追踪
  → 最终生成监管报告，全程可追溯

第四幕：能力展示（1 屏）
  "我们还能量化你的监管效能"
  → 优化模拟器 + TCO 分析
  → 用数据证明 AI 带来的监管效能提升
```

**对应页面路由调整**：

| 当前路由 | 新位置 | 改动 |
|----------|--------|------|
| `/sensing/national-grid` | 第一幕首页 | 重构为副部长 Dashboard |
| `/sensing/regional/:id` | 第二幕 | 保留，但从 Dashboard 下钻进入 |
| `/warning/timeseries` | 第二幕 | 叙事改为 "预测"，不是 "发现" |
| `/warning/enterprise` | 第二幕 | 与 timeseries 合并或作为下钻 |
| `/attribution/workflow` | 第三幕 | 增加前置干预节点 |
| `/attribution/graph` | 第二/三幕 | 作为证据支撑，非独立场景 |
| `/audit/event/:id` | 第三幕 | 保留矩阵结构，增加时限追踪 |
| `/audit/report` | 第三幕 | 结尾，报告生成 |

#### 批评 12：不是闭环

> "全局能看到每一个流程，而不是说只有上一个流程结束之后我才能看到"
> "异常都要先知到处置呢，要形成一个闭环"

**修改方案**：
- 在首页 Dashboard 增加**闭环追踪条**：发现 → 归因 → 分派 → 处置 → 复核 → 归档
- 每个阶段标注当前进度数字
- 点击任意阶段可跳转对应页面

---

### 2.6 商业层面

#### 批评 13：没有 TCO/算力成本的考量

> "你的模型是多大规模的？对应的算力资源我能源部能不能承担得起？"

**涉及页面**：`ReportGeneration.tsx` 或新增

**修改方案**：
- 新增或改造一个 "部署概览" 模块，回答：
  - 模型规模 & 推理延迟
  - 推荐的硬件配置（本土化部署 vs 云部署二选一）
  - 预估年度 TCO
  - 对比传统方案的成本节约（不只是钱，还有事故损失、监管效率）

---

## 三、优先级排序（按演示紧迫度）

周四/周五演示只剩 2-3 天，无法大重构。按投入产出比排序：

### P0 — 不改就翻车（今天必须做完）

| # | 改动 | 工作量 |
|---|------|--------|
| 1 | 补 Pavlodar 煤矿数据 | 30min |
| 2 | NationalGrid 首页重构为副部长 Dashboard（3 个问题） | 3h |
| 3 | PipelineTimeSeries 叙事从 "发现" 改为 "预测" | 2h |
| 4 | 所有数字加上置信区间/方法说明，不可信的数字直接删 | 1h |
| 5 | 新故事线：5 屏闭环而非 8 屏散点 | 2h |

### P1 — 能显著提升说服力（今晚/明早）

| # | 改动 | 工作量 |
|---|------|--------|
| 6 | EnterpriseReporting 增加 "模式匹配/事前预警" 逻辑 | 2h |
| 7 | 视觉精简：减少每屏信息密度，折叠侧面板 | 3h |
| 8 | 首页增加 "有 AI vs 没 AI" 对比指标 | 1h |
| 9 | 修改所有文字从 "我们多快" 到 "帮监管者达成什么" | 1h |

### P2 — 锦上添花（演示前有时间就做）

| # | 改动 | 工作量 |
|---|------|--------|
| 10 | TCO 成本预估模块 | 2h |
| 11 | WorkflowAttribution 增加 "预防措施建议" 节点 | 2h |
| 12 | EventAudit 增加时限追踪和闭环可视化 | 3h |

---

## 四、演示话术建议

配合修改后的 demo，努尔介绍时应按以下逻辑线：

1. **开场**："我们今天展示的不是一个 BI 大屏，而是一个 AI 监管系统——它和传统 BI 的区别是，传统 BI 告诉你发生了什么，我们告诉你即将发生什么、你应该做什么。"

2. **Dashboard**："副部长打开这个页面，3 秒知道——供给稳不稳、哪里有风险、需要我做什么决策。"

3. **预警**："我们不是在事故发生后溯源。我们是基于 AI 时序预测，在事故发生前 48 小时预判，给监管者留出处置窗口。"

4. **归因闭环**："从发现异常到归因到分派到处置到复核，全链路可视、全程留痕——因为监管者自己也在被审计。"

5. **收尾**："这个系统两周可以上线一个场景。不是替代您现有的平台，而是给您的平台装上 AI 大脑。"

---

## 五、努尔核心标准速查

做任何修改时，用这 5 个标准自检：

1. **这是副部长想看的吗？**（不是工程师想看的，不是企业经理想看的）
2. **这是事前预警还是事后追溯？**（事后 = 删掉或降级为历史案例）
3. **有数字就有置信区间吗？**（没有就别放数字）
4. **这一屏只有一个重点吗？**（超过一个 = 太复杂）
5. **这个故事形成闭环了吗？**（发现→归因→分派→处置→审计）
